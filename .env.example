# Telegram Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Telegram Webhook Secret Token (optional, recommended for production)
# Set this to a secure random string and configure it when setting up your webhook
# Using: curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" -d "url=<URL>&secret_token=<SECRET>"
# When configured, the bot will verify the X-Telegram-Bot-Api-Secret-Token header on incoming webhooks
# TELEGRAM_SECRET_TOKEN=your_secure_random_secret_token_here

# LLM Provider Configuration
# Options: "groq" or "deepseek"
LLM_PROVIDER=groq

# Groq API Key (if using Groq)
GROQ_API_KEY=your_groq_api_key_here

# DeepSeek API Key (if using DeepSeek)
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Database Configuration
# For local SQLite (default): leave DATABASE_URL unset or use sqlite:///natal_nataly.sqlite
# For PostgreSQL: use postgresql://USER:PASSWORD@HOST:PORT/DBNAME
# DATABASE_URL=postgresql://user:password@localhost:5432/natal_nataly

# Legacy database path for SQLite (optional, used if DATABASE_URL not set)
# DB_PATH=natal_nataly.sqlite

# Render Deployment Configuration
# Set to "true" when deploying to Render
# RENDER=true

# Webhook URL for Telegram (required when RENDER=true)
# WEBHOOK_URL=https://your-app.onrender.com/webhook

# Port for the application (default: 8000, Render uses 10000)
# PORT=8000

# Debug Mode Configuration
# Enable debug mode for technical debugging information
DEBUG_MODE=false

# Developer Telegram ID (required for debug commands when DEBUG_MODE=true)
# Get your Telegram ID by messaging @userinfobot on Telegram
# DEVELOPER_TELEGRAM_ID=your_telegram_id_here
